import React from 'react';
import { TScreens } from '../../design-system';

// let n = 1;

const ContextScreen = () => {
  // console.log(`Componente ContextScreen renderizou x${n++}`);

  return <TScreens title="ContextScreen Component" />;
};

export default ContextScreen;
